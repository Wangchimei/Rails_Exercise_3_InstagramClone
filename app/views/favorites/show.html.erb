<div class="container">
  <div class="row">
    <div class="col-12">
      <div class="card mt-5">
        <div class="card-body">
          <div class="row">
              <% if @user.favorite_feeds.present? %>
                <% @user.favorite_feeds.reverse_each do |feed| %>
                  <% thumb = feed.image.url :thumb%>
                  <%=image_tag(thumb ,class:"col-sm-3 col-md-3 col-lg-4 px-1 pt-1")%>
                <% end %>
              <% else %>
                <%= content_tag(:div, "現在お気に入りに保存されたフィードはありません。", class: "mt-4 ml-4 text-muted h6-5")%> 
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>